{
  "name": "@hashnotes/app",
  "version": "0.1.0",
  "type": "module",
  "main": "src/main.ts",
  "scripts": {
    "check": "tsc --noEmit",
    "dev": "bun run scripts/dev.ts",
    "build": "bun run scripts/build.ts",
    "preview": "bun run scripts/preview.ts"
  },
  "devDependencies": {
    "esbuild": "^0.25.10"
  },
  "dependencies": {
    "@hashnotes/core": "*",
    "@hashnotes/lib": "*"
  }
}
